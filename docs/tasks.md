# Block 6 — Multi-Agent Clinical Cohort System — Tasks

Checklist form of `docs/plan.md`. Each phase = its own branch + PR, per project convention. Phase 1 is this one (spec + plan + tasks, all on `phase-1-spec`).

## Phase 1 — Spec, Plan, Tasks (branch: `phase-1-spec`)

- [x] `docs/spec.md` committed
- [x] `docs/plan.md` committed
- [x] `docs/tasks.md` committed (this file)
- [x] Re-verify Block 3/4/5's current interfaces against `docs/spec.md` §5 before moving to Phase 2. `run_agent`'s signature, `ClinicalAnswer`'s fields (`rag_citations` included), `QuestionInput`'s fields, `top_k=25`, and confidence tiers all match against Block 4's and Block 5's `main` branches. Block 4's `/query` contract, both 10s timeouts, and the `sources[].chunk_text` field all match. Block 3's relationship names/env-var convention match. Phase 2 is unblocked.
- [x] **The timeout Block 5's `rag_tool.py` sets on its own outbound HTTP call to Block 4's `/query` endpoint:** `timeout=10` (`rag_tool.py:60`) — tighter than the 20s/attempt Block-4-internal ceiling, not looser. §7's worst-case math: ~80s theoretical worst case for Role 1. The 150s branch timeout stays as generous headroom over that.
- [x] Amend `docs/spec.md` §2 to drop the second return value from `run_cohort_agent`'s signature (plan.md §1 — the `bool` had no defined meaning for Role 2's single-step design)
- [x] **Spec cleanup:** now that `plan.md` has resolved the execution-model, driver-lifecycle, and node-wrapper items spec.md's own scope note flagged, confirm spec.md's corresponding paragraphs stay trimmed to the constraint level with pointers to `plan.md` (already done in this pass) — don't let detail creep back into spec.md as this repo evolves. Re-verified: §2's driver-lifecycle constraint, execution-model constraint, and node-wrapper note are each one pointer sentence to `plan.md` §6/§4/§5, no restated mechanics.
- [x] Patient data used across Blocks 3–6 is synthetic/de-identified, not real PHI (plan.md §16). Phase 5 is clear to trace full state to LangSmith as-is.
- [x] **`Citation.snippet`'s data source:** Block 5's `ClinicalAnswer.rag_citations` (`{patient_id, chunk_id, snippet}`, via `build_rag_citations()`) is the source — no placeholder or Block 4/5 change needed. `docs/spec.md` §2/§3 reflect this. Phase 2 writes `test_schemas.py`/`test_orchestrator.py` `Citation` cases against this real field, not a stub.
- [x] Push `phase-1-spec`, open PR covering spec + plan + tasks together

## Phase 2 — TDD (branch: `phase-2-tdd`)

Write failing tests first, against fakes — no live Neo4j/RAG/LLM calls in this phase.

- [x] `scripts/schemas.py`: define `CohortResult`, `ReconciliationResult`, `Citation`, `MultiAgentAnswer`, `MultiAgentState` (plan.md §2's file layout, spec.md §3's field lists)
- [x] `tests/test_schemas.py`: validate field types, especially `CohortResult.question: QuestionInput` vs `MultiAgentAnswer.question: str` (spec.md §3 — don't let these drift to the same type by accident)
- [x] `tests/test_cohort_agent.py`: fakes for `graph_query_fn`/`count_fn`, covering:
  - `outcome="answered"` happy path
  - `outcome="nothing_found"` (zero matches)
  - `outcome="tool_error"` after exhausting `_MAX_TOOL_RETRIES` retries (plan.md §7's 10s-per-attempt assumption, verified against fake timing, not real Neo4j)
  - never raises even when the fake raises on every attempt
- [x] `tests/test_cohort_tool.py`: assert the Cypher string is parameterized (no f-string/`.format()` interpolation of `condition`/`lab`/`drug_a`/`drug_b` into the query text itself) and contains only `MATCH`/`RETURN` (spec.md §2's security constraints) — a static string-inspection test, not a live DB test
- [x] `tests/test_orchestrator.py`: fakes for both `run_agent` and `run_cohort_agent`, covering every row of spec.md §4's degradation matrix and every bullet of §2's reconciliation rules:
  - both `answered`, `total_patients_matched <= 25`, counts match → `mode="reconciled"`, `confidence="high"` per plan.md §8's redefinition
  - both `answered`, `total_patients_matched > 25` → Role 2's counts authoritative, `mode="reconciled"`, `confidence="high"`
  - both `answered`, counts disagree despite `<= 25` → `confidence="low"`, `discrepancy_flag=True`, `authoritative_source="neither"` (plan.md §8's `low` tier)
  - one `nothing_found`, other `answered` → same as above, plus assert `ReconciliationResult.notes` reflects the actual `get_known_vocabulary()` lookup result (plan.md §12), not a generic guess string
  - both `nothing_found` → `mode="reconciled"`, zero patients, no discrepancy
  - `clinical_error` (tool_error) + cohort succeeds → `mode="cohort_only_degraded"`, answer text from plan.md §15's template, no citations, and explicitly assert `confidence="high"` (plan.md §8 — Role 2's exhaustive count is the sole source here, and it's exhaustive regardless of Role 1's availability; don't let this accidentally fall through to `medium`/`low` since Role 1 produced nothing to check a count against)
  - clinical succeeds + `cohort_error` (tool_error) → `mode="clinical_only_degraded"`, and explicitly assert `confidence="medium"` when `len(clinical_result.rag_patient_ids) >= 15`, or `confidence="low"` when it's below 15 (plan.md §8's corrected, broadened `medium` band) — cover both sides of that boundary as separate test cases, not just one
  - both `tool_error` → `mode="both_failed"`, `confidence="low"`, never raises
  - `clinical_count_step_ran=False` → treated as no comparable count, not a `0` (spec.md §3) — assert this explicitly, don't just assume it falls out of other logic
  - a fake that raises an exception outside the agents' own documented contract → caught by the node wrapper's own try/except (plan.md §5), classified via `classify_exception`, never crashes the graph
- [x] `tests/test_error_classification.py`: `classify_exception` maps each of the four exception categories from plan.md §5 correctly
- [x] `tests/test_orchestrator.py` — timeout-specific cases (plan.md §4's thread-leak note): assert a branch that exceeds the 150s ceiling reports `"timeout"` correctly via the dedicated `block6_executor`, and assert a late-arriving result from an already-timed-out call is logged as a warning rather than silently dropped
- [x] Session-scoped pytest fixture that calls `driver.close()` after the full test session (plan.md §6's teardown note) — confirm no lingering connections after a test run
- [x] Confirm all of the above fail for the right reason (no implementation exists yet) before moving to Phase 3: `test_schemas.py` (22 tests) passes since `scripts/schemas.py` already exists; `test_cohort_agent.py`, `test_cohort_tool.py`, `test_error_classification.py`, and `test_orchestrator.py` each fail collection with a `ModuleNotFoundError`/`ImportError` naming the specific Phase 3 module still missing
- [x] `requirements.txt` pins `block5_agent` as a git dependency against Block 5's `main` (now pip-installable per Block 5's own packaging PR #11, merged) — confirmed `pip install -r requirements.txt` pulls it in cleanly alongside this repo's own deps
- [x] Push `phase-2-tdd`, open PR (PR #2, against `phase-1-spec`)

## Phase 3 — Implement (branch: `phase-3-implement`)

- [x] `scripts/cohort_tool.py`: the enumeration Cypher (one-clause edit of Block 5's `VERIFY_PATIENTS_QUERY_TEMPLATE` per spec.md §2 — drop `AND p.person_id IN $person_ids`), exhaustive drug-count query, `get_driver()` factory (plan.md §6 — cached, long-lived, injectable)
- [x] `scripts/cohort_agent.py`: `run_cohort_agent` — plain retry loop (no internal LangGraph; single-step, doesn't need one), 2 retries, 10s Cypher timeout (plan.md §7), returns `CohortResult` only (plan.md §1's corrected signature)
- [x] `scripts/error_classification.py`: `classify_exception` implementation
- [x] `scripts/orchestrator.py`: `MultiAgentState` graph — `dispatch` → async `clinical_node`/`cohort_node` (via a dedicated `block6_executor` `ThreadPoolExecutor`, not bare `asyncio.to_thread`, wrapped in `asyncio.wait_for(..., timeout=150)` per plan.md §4/§7, with a warning logged if a timed-out call's thread later completes anyway) → `reconcile_node` (implements spec.md §2's rules + plan.md §8's confidence redesign + §15's answer-text template + §12's runtime `get_known_vocabulary()` call, with a TTL cache refresh, on any `nothing_found`/`answered` split) → both `run_multi_agent` (sync) and `run_multi_agent_async` (plan.md §4 — required for Block 8's future async FastAPI integration, not optional) entry points, with the sync version delegating to the async one. Also added `scripts/vocabulary_check.py`'s `get_known_vocabulary()` (its Phase-4-listed CI entry point is still to come), since `reconcile_node` needed a real function to import and tests to monkeypatch, not just a stub.
- [x] Run Phase 2's test suite against this implementation — all green, no test edits to make them pass (if a test needs changing, that's a signal the test was wrong in Phase 2, fix it there with its own commit, don't quietly adjust it here). One Phase 2 bug found and fixed in its own commit: `test_clinical_count_step_ran_false_is_not_treated_as_a_zero_count` was missing a `get_known_vocabulary` monkeypatch, so it was silently making a real Neo4j call. 64/64 tests pass, stable across repeated runs.
- [x] Push `phase-3-implement`, open PR (PR #3, against `phase-2-tdd`)
- [x] **Followup hardening, pushed to the same PR #3:** `reconcile_node` is now wrapped the same way `clinical_node`/`cohort_node` already were (plan.md §5) — a real, unmonkeypatched `get_known_vocabulary()` failure (its `get_driver()` boundary faked, not the function itself) now degrades to `mode="both_failed"` instead of crashing the graph. The asymmetric-split routing check gained its missing mirror case — clinical `answered` + cohort `nothing_found` — which previously fell through to `_both_answered_reconciled_answer` and could silently read as "both agree" whenever both sides' drug counts happened to be 0/0; it now routes to `_vocabulary_split_answer` like the already-handled direction, so the vocabulary-drift note fires both ways. `cohort_tool.py`'s `query_full_cohort`/`count_drugs_exhaustive` now call `get_driver()` inside their own `try` block, so a connection failure there is caught as `CohortServiceError` too, not raised raw. `cohort_agent.py`'s `_call_with_retries` now catches any exception, not just `CohortServiceError`, converting anything else into one first — `run_cohort_agent`'s "never raises" contract now holds standalone. 70/70 tests pass, stable across repeated runs.

## Phase 4 — Data & Validation (branch: `phase-4-data`)

- [x] Copy `data/seed/ci_graph_seed.cypher` from Block 5's current file (plan.md §10 — same frozen snapshot, not regenerated)
- [x] Copy the 8 fixed questions into `data/eval/questions.json`, sourced from Block 5's `data/eval/tasks.json` (the actual question fields, despite the different filename — Block 5 has no file literally named `questions.json`)
- [x] **Ground-truth re-verification (spec.md §6, plan.md §11):** run the new unbounded enumeration query once, by hand, against the seed data for the two questions Block 5's eval flagged as top_k-capped. Record the real total patient count and real drug_a/drug_b split for each. Update `data/eval/answer_key.json` with these independently-verified numbers — do not reuse Block 5's existing capped answer as ground truth for these two questions. Done via cypher-shell against a disposable, isolated Neo4j container loaded with the seed (never against Block 3's shared local `neo4j-omop` container). Result at the time: q1 and q7's real exhaustive counts were both exactly 25 — identical to Block 5's original capped numbers for that seed, independently confirmed rather than assumed. **Superseded for q1 by Phase 7:** the seed itself was later found to be too small to expose the real gap; see Phase 7 below — q7's 25 stands unchanged.
- [x] `scripts/vocabulary_check.py` (plan.md §12): the CI-time entry point, plus the shared `get_known_vocabulary()` function it's built on — cross-reference Block 3's distinct `condition_name`/lab property values against `data/eval/questions.json`'s exact strings for the CI-time check; the same function is what Phase 3's `reconcile_node` calls at runtime for any question, not just the fixed 8
- [x] Run `vocabulary_check.py` against the real seed data now, before wiring it into CI — confirm it actually passes against the current questions, or fix the mismatch it finds. Passes cleanly against the real 8 questions; also verified the failure path itself works by deliberately breaking a condition/lab value in a scratch copy and confirming both mismatches were caught and named.
- [ ] ~~Provision the `block6_readonly` Neo4j role/user (plan.md §6) with read-only privileges on the relevant labels; wire its credentials into this repo's env config, separate from Block 3's load-time credentials~~ **Blocked, not an oversight:** this project runs Neo4j Community Edition everywhere (`neo4j:5.18-community`, matching Block 3's `docker-compose.yml`), which has no RBAC support at all — `CREATE ROLE`/`GRANT`/`SHOW ROLES` are Enterprise-only. Confirmed directly: `SHOW ROLES` against a real instance of this exact image fails with `Unsupported administration command`, and `CALL dbms.components()` reports `edition: "community"`. There is no role to provision on this stack. See plan.md §6 and tasks.md Phase 6 for the resulting scope decision and follow-up note.
- [x] Push `phase-4-data`, open PR (PR #4, against `phase-3-implement`)

## Phase 5 — CI & Eval Harness (branch: `phase-5-ci`)

- [x] GitHub Actions workflow: spin up a disposable Neo4j instance, load `ci_graph_seed.cypher`, run `vocabulary_check.py` first (fail fast if it doesn't pass), then the test suite from Phase 2/3
- [x] **Decide explicitly (plan.md §13's flag): do the 11 fixed questions run sequentially or concurrently in the eval harness?** Decided: concurrently, via `asyncio.gather` in `scripts/run_eval.py`. Documented inline why plan.md §13's rate-limit concern doesn't apply under CI's specific `USE_RAG_FIXTURES`+`USE_STUB_ANSWER_FN` configuration (zero real external calls from Role 1; Role 2 only hits the local disposable Neo4j container) — a future run against real services would need to revisit this.
- [x] Eval harness (`scripts/run_eval.py`): runs the 8 recall-scored fixed questions through `run_multi_agent_async` (not the sync wrapper, since the harness itself needs to run concurrently), scores recall against the (now corrected) `answer_key.json`, with q1/q7 (the two previously-capped questions) scored against their Phase-4-verified ground truth
- [x] **q9–q11 pass/fail check (not recall-scored):** implemented in `scripts/run_eval.py` — separate pass/fail per question, reported apart from the 8-question recall percentage
- [x] Eval dimension for graceful degradation (spec.md §6): `scripts/run_eval.py`'s `_check_degradation_matrix()` — fakes (not shared with `tests/test_orchestrator.py`, duplicated on purpose to keep the eval harness independent of the test suite's layout) exercise all 4 matrix rows, asserting correct `mode`
- [x] Discrepancy scoring (plan.md §14): any `discrepancy_flag=True` on the 8 fixed questions fails the build, naming the question ID(s), reported separately from the recall metric
- [x] `scripts/run_log.py` (plan.md §9): wired into `run_multi_agent_async` — every invocation (eval and otherwise) appends to `data/eval/run_log.jsonl`. Also required threading `run_agent`'s new `cost_info` return value (Block 5's `phase-11-expose-cost` branch, not yet merged to `main` — see `requirements.txt`'s provisional pin) through `clinical_node` and `MultiAgentState.clinical_cost_info`
- [x] Regression gate: `scripts/run_eval.py` compares this run's recall against `data/eval/latency_baseline.json`, failing the build on a drop; p95 latency delta is reported alongside it (not a hard gate — no latency threshold exists anywhere in spec.md/plan.md to gate against)
- [x] **Measure real latency** (plan.md §13): `data/eval/latency_baseline.json` committed from a real, cold-start local run against a fresh disposable Neo4j container (recall=1.0, p95_latency_ms=5282.0) — this measured number, not plan.md's ~5–12s estimate, is what future runs are checked against
- [x] Report in the PR description, all as real measured output from this CI run, not targets/estimates from spec.md or plan.md: before/after recall on the two previously-capped questions, median/p95 latency, and total/average cost + token usage across the eval suite
- [x] Push `phase-5-ci`, open PR (PR #5, against `phase-4-data` — not merged, pending Block 5 PR #12's `phase-11-expose-cost` review)
- [x] **Followup, pushed to the same PR #5:** merged `phase-4-data` forward (its 4 Phase-3 hardening fixes plus its own `vocabulary_check.py` lab-check fix) — clean merge, but surfaced 3 tests still built on the old 2-tuple `clinical_agent_fn` fake shape, fixed in their own commit; re-ran the full CI sequence post-merge, no regressions, 75/75 tests pass. Then two more fixes from a PR #5 review, each its own commit: (1) `run_eval.py`'s latency/cost baseline was diluted by the degradation-matrix eval dimension's 4 near-instant fake-driven log entries getting swept into the same stats as the real question runs — fixed via a second log-offset captured right after the real runs finish, before the degradation matrix runs (`_read_log_entries_between`, no schema change to `run_log.py`'s entries); (2) no question in Block 5's original 11-question set exercises `total_patients_matched > 25` with a non-zero real drug count (q1/q7 both sit exactly at the 25-patient boundary) — closed by extending `data/seed/ci_graph_seed.cypher` with an isolated 30-patient cohort (see `docs/plan.md` §10) and adding it as **q12** (12 total / 9 scored), independently re-verified by hand (`total_patients_matched=30, drug_a_count=15, drug_b_count=5`), with every other "8 scored"/"11 total" assumption in `docs/spec.md` §6, `docs/plan.md` §12/§13/§14, and `scripts/run_eval.py`/`scripts/vocabulary_check.py` updated to match (several made count-agnostic instead of re-hardcoded, so this doesn't silently drift again). Real recall after all of the above: **9/9 (1.000)** across all scored questions, all q9–q11/discrepancy/degradation-matrix checks still pass, `data/eval/latency_baseline.json` updated to this run's real measured p95 (953.0ms).

## Phase 6 — Docs & Wrap-up (branch: `phase-6-docs`)

- [x] README: problem, architecture diagram (the dispatch → two branches → reconcile shape from plan.md §3), tech stack, results (real recall numbers from Phase 5), AI-assisted workflow note
- [x] "What I'd do next" section (mirroring Block 5's own pattern): added as `spec.md` §9, not README.md — Block 5's actual section lives in its `docs/spec.md`, not its README, so matched that placement rather than the assumption it belongs in the README. Covers the cost short-circuit deferred in plan.md §17, DB-level permission hardening reworded to "provision `block6_readonly` on Neo4j Enterprise Edition or an equivalent managed offering with RBAC" (Phase 4 found Community Edition isn't buildable at all, not just under-hardened), PHI-in-traces redaction if plan.md §16's synthetic-data assumption stops holding, the still-needed process-level driver shutdown hook, and a new item Phase 5 surfaced — the inherited $/token cost rate is unverified against Anthropic's current pricing. **Since resolved — see Phase 9: the rate was checked and confirmed current, this item is closed, not a remaining action.**
- [x] Confirm every item in spec.md §7's Acceptance Criteria is actually true against the committed code, not just assumed — go through the list one by one. Result at the time of this check (Phase 6, before Phase 7's seed fix and Phase 9's tracing verification): 5 of 7 fully true and directly verified against real code/test runs (LangGraph orchestrator structure; `run_agent` reused, never reimplemented — confirmed via `grep`, zero local reimplementation; `MultiAgentState` is a plain `TypedDict`, 10 explicit fields, no hidden state; degradation matrix — re-ran `_check_degradation_matrix()` fresh, all 4 rows pass; Cypher parameterized/read-only — re-ran `test_cohort_tool.py` fresh, 9/9 pass). 2 of 7 only **partially** true at the time of this check, noted plainly rather than rounded up — both have since been fully resolved, see below:
  - ~~"Traced and evaluated... LangSmith tracing" — the eval/regression-gate/cost-logging pieces are real and verified (Phase 5), but LangSmith tracing itself is only env-var-configured (`.env.example`), never actually exercised against a real LangSmith account anywhere in this project — CI deliberately runs with `LANGCHAIN_TRACING_V2` unset (see `ci.yml`'s own comment) to avoid a second secret. The capability exists; it has not been observed working.~~ **Since resolved — see Phase 9: tracing was exercised for real (one real question run end-to-end, no stubs), and confirmed via two live trace IDs and their LangSmith URLs, not just the dashboard. This criterion is now fully true, not partial.**
  - "Measured recall on the two previously top_k-capped questions... improves" — **at the time of this check**, q1/q7's raw numbers (25/10/8 and 25/0/0) were **identical** before and after, because Block 5's original RAG-capped count happened to exactly equal that seed's true population — a coincidence of the seed's construction, not evidence the fix mattered. Phase 7 found exactly that: the seed was too small to expose the real gap. **Superseded by Phase 7: this criterion is now fully true, not partial** — with q1's seed corrected to its real 99-patient population, Block 5's own RAG-capped answer permanently caps at 25/99 while Block 6's reconciled answer gets all 99 correctly, a real demonstrated delta, not a coincidence of a too-small seed.
  - **Final tally after Phase 7 and Phase 9: 7 of 7 fully true.** Every acceptance criterion in spec.md §7 is now verified true against real, current code/data/traces — none partial.
- [x] Final PR, tag for Block 7 to pick up (threat model against this repo's new Cypher surface and the security constraints already flagged in spec.md §2) — PR #6, against `phase-5-ci`, not merged. No repo label existed for this handoff; flagged directly in the PR title/body instead of inventing one.
- [x] **Followup, pushed to the same PR #6:** merged `phase-5-ci` forward (its dependency-pin fix, `run_eval.py`'s log-offset dilution fix, and q12's isolated seed extension — see Phase 5's own followup entries above) — clean merge, 78/78 tests pass (up from this phase's original 65/65), no regressions. Re-walked the two bullets above against the now-current code/data rather than leaving the original walkthrough's numbers stale: `test_cohort_tool.py` is now 11/11 (not 9/9 — Phase 3's followup hardening added 2 more driver-failure tests after this walkthrough was first written); recall is now measured across **9 scored questions (9/9, 1.000)**, not 8, since q12 was added specifically to close a gap this same walkthrough's own "structural improvement, not a delta" framing exposed — q1/q7 sit exactly at the 25-patient boundary and never actually exercised the `total_patients_matched > 25` path against a real non-zero drug count; q12 does (30/15/5, independently re-verified). The LangSmith-tracing partial-true caveat is unchanged — still config-only, never exercised against a real account. README.md's results table and this file both updated to match; `docs/spec.md` §6's "8 scored"/"11 total" language was already corrected on `phase-5-ci` before this merge.

## Phase 7 — Fix q1's seed (branch: `phase-7-fix-q1-seed`)

Block 5 found its own CI seed's q1 population (Essential hypertension, SBP > 140) was too small to ever expose the real gap between what RAG's `top_k=25` search can return and the true matching population: the seed originally carried only the exact 25 patients RAG's own search returns, so the golden answer and the agent's necessarily-capped output could never disagree, no matter how large the true population really was. Fixed in `genai-block5-agent`'s `phase-12-fix-q1-seed` branch (PR #13, open, not yet merged to Block 5's `main`) — true population is 99 patients, not 25. Mirrored here, since Block 6's own seed is a copy of Block 5's.

- [x] Re-copy `data/seed/ci_graph_seed.cypher` from Block 5's `phase-12-fix-q1-seed` (same verbatim-copy pattern as Phases 4/5) — brings in the true 99-patient population
- [x] Confirm `data/eval/rag_fixtures.json` is still byte-identical to Block 5's copy — direct `diff`, not assumed (Block 5 didn't touch this file for its fix). Confirmed: no diff.
- [x] Update `data/eval/answer_key.json`'s q1 entry to 99/49/28 — independently re-verified against Block 6's own loaded copy of the corrected seed (not trusted just because the source file was copied), via both raw Cypher (cypher-shell) and the real `run_cohort_agent` code path. Both agree: 99 total, Lisinopril 49, Amlodipine 28.
- [x] Re-ran the eval harness for real against the corrected seed: q1 now passes (`mode="reconciled"`, `confidence="high"`, `total_patients=99`, matching the corrected ground truth) — the `total_patients_matched > 25` branch (tested with fakes back in Phase 2/3) fires for real for the first time. Contrast with Block 5: Block 5's own RAG-capped search still only returns 25 of the 99 patients, so Block 5 now fails this question's accuracy check permanently, by its own design (see its PR #13) — this is exactly the gap Block 6 exists to close, now demonstrated against real data, not just asserted.
- [x] Reset `data/eval/latency_baseline.json` — new baseline: recall=1.0, p95_latency_ms=1031.0, measured the same way as the original (cold-start disposable container, real CI step sequence). **Investigated, don't just trust the number:** the ~5x drop from Phase 5's original baseline (p95 5282ms) initially looked explained by q1's population growing from 25 to 99 patients, but a controlled A/B check (identical fresh-container methodology, old 25-patient seed vs. new 99-patient seed) found population size is *not* the explanation — both land in the same ~1.0-1.4s p95 range, consistent with Neo4j's relationship traversal making 25 vs. 99 matching rows negligible either way (no index on `condition_name`, but traversal cost scales with matching-patient count via index-free adjacency, not graph size, and both counts are tiny regardless). Phase 5's 5282ms was most likely a one-off cold-start outlier for that specific run, not a stable baseline. Corrected the file's own justification note, which had originally (wrongly) attributed the change to the population increase.
- [x] Corrected the stale Phase 4/6 notes claiming q1's true count "is exactly 25, identical to Block 5's original capped count" — that was accurate for the seed as it existed then, not a permanent fact; updated in place rather than left to sit next to the new truth.
- [x] Push `phase-7-fix-q1-seed`, open PR against `phase-6-docs` (PR #7, not merged, same as every prior phase — mirrors Block 5's own still-open PR #13)
- [x] **Followup, pushed to the same PR #7:** merged `phase-6-docs` forward (its own merge of `phase-5-ci`, carrying q12, the dependency-pin fix, and the log-offset dilution fix). 4 real conflicts, resolved by hand rather than a blind strategy: `data/seed/ci_graph_seed.cypher` (both patient sets kept - verified zero `person_id` collisions between q1's 74 new patients and q12's 30 before merging, 206 unique patients total), `data/eval/latency_baseline.json` and `README.md`'s results section (combined rather than picked between - neither side's pre-merge content reflected the other's addition), and `docs/tasks.md` (phase-6-docs's Phase 6 followup bullet re-homed under Phase 6, Phase 7's section kept intact). Re-verified ground truth for **all 12 questions**, not just q1, against the fully-merged seed via the real enumeration + drug-count Cypher (same by-hand discipline as every prior phase) - every one matches `answer_key.json` exactly, including q7 and q12 confirmed unshifted despite the seed growing twice (102 → 206 patients). Ran a fresh eval harness pass against the merged state rather than reusing either PR's pre-merge numbers: **9/9 recall (1.000)**, all q9-q11/discrepancy/degradation-matrix checks pass, real combined latency baseline median 882.5ms / p95 891.0ms. Upgraded spec.md §7's recall criterion from "2 of 7 partial" to "6 of 7 fully true, 1 of 7 partial" - q1's recall improvement is now a real, demonstrable delta (Block 5 permanently caps at 25/99; Block 6 gets all 99), not merely a "structural improvement in confidence" as originally written before this merge.

## Phase 8 — Hardening (branch: `phase-8-hardening`)

Four small, independent hardening fixes surfaced by review, not a new feature phase.

- [x] **Preserve real exception detail; stop defaulting to always-retryable.** `scripts/cohort_tool.py`'s `query_full_cohort`/`count_drugs_exhaustive` now call `classify_exception(exc)` to decide `retryable` ("timeout"/"connection_error" → `True`; "validation_error"/"unknown" → `False`, a bug or bad input that retrying won't fix), instead of unconditionally defaulting to `retryable=True`. Pass `str(exc)` as the detail, not `type(exc).__name__`, so the real message reaches the final caveat text. `tests/test_cohort_tool.py` had zero behavioral coverage of these functions' exception handling before this (static string-inspection only) — added fake-driver tests for both a retryable and a non-retryable ("unknown") case, on both functions. `tests/test_cohort_agent.py` gained a test tying the "unknown" classification to the agent-level single-attempt contract explicitly.
- [x] **Retry backoff.** `cohort_agent.py`'s `_call_with_retries` now sleeps `0.5s * attempt_number` between retryable failures (0.5s, then 1.0s) instead of retrying 3 times back to back with zero delay. `sleep_fn` is injectable (defaults to real `time.sleep`), threaded through `run_cohort_agent`, so tests pass a no-op fake instead of actually waiting — a dedicated test records the real delay values via a fake `sleep_fn` to prove the backoff itself is correct, not just skippable. Block 5's `agent.py` has the identical zero-delay-between-retries gap and was **not** touched here — flagged as a separate, optional follow-up for Block 5's own repo, not silently inconsistent.
- [x] **Injectable `GRAPH_QUERY_TIMEOUT`.** Added `graph_query_timeout=None` keyword arg to both `query_full_cohort`/`count_drugs_exhaustive`, defaulting to the module constant, matching the existing `driver=None` DI pattern. Extended the fake driver in `test_cohort_tool.py` to record the `Query` objects `session.run()` was called with, so tests assert `.timeout` directly (matching Block 5's own `test_graph_tool.py` precedent) rather than trusting the wiring.
- [x] **Documented `run_eval.py`'s real concurrency constraint.** The existing comment justifying concurrent execution only reasoned about external rate limits (which fixtures avoid). Added a note that `block6_executor` is a single shared, 8-worker `ThreadPoolExecutor` (sized for one question's 2 branches at a time, not a whole eval sweep), so the up-to-22 concurrent branch calls across 11 questions mostly queue rather than run simultaneously — not a bug, but worth naming so a future reader doesn't assume true 22-way parallelism from `asyncio.gather` alone.
- [x] 75/75 tests pass throughout (71 → 75, four new/updated test files, no real added sleep time in the suite)
- [x] Push `phase-8-hardening`, open PR against `phase-7-fix-q1-seed` (PR #8, not merged, same as every prior phase)
- [x] **Followup, pushed to the same PR #8:** merged `phase-7-fix-q1-seed` forward (its own merge of `phase-6-docs`, carrying q12, the 99-patient q1 fix, the dependency-pin fix, and the log-offset dilution fix). 5 conflicts, resolved by hand: `scripts/cohort_tool.py` (this branch's pre-merge code still constructed `driver`/`timeout` *before* the `try` block - phase-3's followup fix moving `get_driver()` inside `try` hadn't reached this branch yet; resolved by computing both inside `try`, combining the connection-failure fix with this phase's own timeout-injection feature), `scripts/cohort_agent.py` and `tests/test_cohort_tool.py` (pure docstring/additive-test conflicts, both sides' content kept), `scripts/run_eval.py` and `docs/tasks.md` (stale "11 questions/22 calls" language updated to "12 questions/24 calls" to match q12, phase-6-docs's own followup bullet re-homed under Phase 7 where it belongs). Full suite: **88/88 pass post-merge** (up from this phase's original 75/75) - Phase 8's retry/backoff/timeout work holds against the corrected 99-patient q1 seed with no adjustment needed.
- [x] **Fixed a PR #8 review's regression finding: `classify_exception` had no case for real neo4j exception types this phase's own retry logic depends on.** `scripts/error_classification.py` only recognized `asyncio.TimeoutError`/`ServiceUnavailable`/`ConnectionError`/`httpx.ConnectError`/`ValidationError` - a real `Query(timeout=...)` expiring server-side raises `neo4j.exceptions.ClientError` with code `Neo.ClientError.Transaction.TransactionTimedOut`, not a `ServiceUnavailable`, so it fell through to `"unknown"` and was wrongly marked non-retryable by this phase's own new retryable-classification logic. Added: that specific `ClientError` code → `"timeout"` (any other `ClientError` code stays `"unknown"` - a syntax error isn't a timeout); `TransientError` (leader switch, deadlock) → `"timeout"`; `SessionExpired` → `"connection_error"`. Regression test reproduces the exact scenario end-to-end - a real `ClientError` (constructed via `Neo4jError._hydrate_neo4j`, the driver's own real construction path, not a hand-rolled fake) raised from a fake driver's `session.run()`, through `query_full_cohort`, asserting `CohortServiceError.retryable is True` - not just the existing generic `ConnectionError`/`RuntimeError` fakes. Confirmed RED first by temporarily reverting the fix and re-running: all 4 new tests failed for the right reason, including the integration-level regression test. 93/93 tests pass with the fix restored.

## Phase 9 — Verification follow-ups (branch: `phase-9-followups`)

Three independent follow-ups from the real LangSmith tracing verification, not a new feature phase.

- [x] **Filed the LangSmith 401 bug upstream.** Reproducible symptom found during tracing verification: `Client.list_runs()`/`read_project()` (both go through `request_with_retries()` → `GET /sessions`) reliably 401 with `{"detail":"Invalid token"}` using a valid key, while a raw `requests.get()` with the identical header succeeds. Ruled out a bad key (`client.info`/`list_projects()` succeed with the same key; a real trace was confirmed recorded via the raw-HTTP path). Downloaded and diffed the `langsmith` wheels for 0.10.6 and 0.10.11 (the full range compatible with `langgraph==1.2.9`'s transitive `langsmith>=0.3.45,<1.0.0` constraint) — `read_project()`/`request_with_retries()` are byte-identical across that whole range, so no version bump fixes this. Filed as [langchain-ai/langsmith-sdk#3279](https://github.com/langchain-ai/langsmith-sdk/issues/3279). Nothing to commit in this repo for this one; `requirements.txt` stays untouched (confirmed, not just left alone by default).
- [x] **Documented the two-root-traces quirk** in `spec.md` §6: Block 5's `run_agent` runs inside Block 6's `clinical_node` thread-pool call, and LangSmith trace context doesn't propagate across that boundary — a real run produces two separate root traces per question (correlate by timestamp), not one nested trace. Confirmed directly during the tracing verification (two root `LangGraph` runs, both starting within ~0.5s of the same triggering call). Expected behavior, not a bug.
- [x] **Verified the cost-per-token rate — closed, not stale.** Checked Anthropic's own published pricing docs (`platform.claude.com/docs/en/about-claude/pricing`) for `claude-sonnet-4-6` (the model this repo actually calls, via Block 5's `agent.py`): **$3/MTok input, $15/MTok output** — an exact match to Block 5's `logging_utils.py` constants this repo's `scripts/run_log.py` inherits its cost accounting from. No drift found, so no code change. Updated the caveat language in `README.md` and `spec.md` §9 (previously "unverified, treat as directional") to reflect this is now checked and current, closing out an item that had been carried forward unverified since Phase 5.
- [x] 75/75 tests pass throughout (docs-only changes this phase, no test/behavior changes)
- [x] Push `phase-9-followups`, open PR against `phase-8-hardening` (PR #9, not merged, same as every prior phase)
