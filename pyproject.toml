[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "block6_multiagent"
version = "0.1.0"
description = "Two-agent LangGraph system reconciling RAG-grounded and exhaustive graph-enumeration answers to clinical cohort questions"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["scripts"]
